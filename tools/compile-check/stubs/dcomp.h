#pragma once
// compile-check stub — the real header comes from the Windows SDK.
// DirectComposition interfaces appear only as ComPtr<> members in Renderer.h;
// the calls live in Renderer.cpp, which is Windows-only and excluded.

struct IDCompositionDevice;
struct IDCompositionTarget;
struct IDCompositionVisual;
