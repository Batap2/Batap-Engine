#pragma once
// compile-check stub — see tools/compile-check/README.md.
// Empty on purpose: the harness force-includes stubs/prelude.h, which pulls
// DirectX-Headers' winadapter and the few extras the engine needs.
